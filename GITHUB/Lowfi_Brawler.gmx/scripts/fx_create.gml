///fx_create(x,y,sprite_index,image_speed)

with instance_create(argument0, argument1, obj_fx)
{    
    sprite_index = argument2;
    image_speed = argument3;
    depth = other.depth;
    return id;
}
    
    
